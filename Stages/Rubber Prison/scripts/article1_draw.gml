draw_sprite(sprite_get("cage"), 0, x, y);

// draw_debug_text(x, y, string(state));

// draw_circle_color(get_marker_x(1), get_marker_y(1), 1, c_red, c_red, false);
// draw_circle_color(x, y, 1, c_blue, c_blue, false);