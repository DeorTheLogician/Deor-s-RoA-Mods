// var element_color = player_id.stance_colors[element];

// gpu_set_blendmode(bm_add);
// draw_circle_color(x, y - (bbox_bottom - bbox_top)/2, radius, element_color, c_black, false);
// gpu_set_blendmode(bm_normal);

// draw_circle_color(x, y - (bbox_bottom - bbox_top)/2, radius, c_red, c_red, true);