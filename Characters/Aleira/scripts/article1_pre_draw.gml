// shader_end();

// var element_color = player_id.stance_colors[element];
// var rad = ease_bounceInOut(floor(radius * 0.67), floor(radius * 1.5), floor(image_index) % 5, idle_anim_length - 1);

// gpu_set_blendmode(bm_add);
// draw_circle_color(x, y - (bbox_bottom - bbox_top)/2, rad, element_color, c_black, false);
// gpu_set_blendmode(bm_normal);